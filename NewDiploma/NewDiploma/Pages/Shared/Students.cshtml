@{
    ViewData["Title"] = "Студенты";
}
<div class="groups">
    <div class="group-left g-width">
        <div class="group-search">
            <input class="input-style" type="text" placeholder="Введите номер группы" name="groupName" id="txtCustomerName" />
        </div>
        <div class="group-list">
            @foreach (var item in @Model.Groups)
            {
                <a class="group-action" href="@Url.Action("Students", "Home", new{groupName = @item.GroupName})">
                    <div class="group-item">
                        <span>@item.GroupName</span>
                    </div>
                </a>
            }
        </div>
    </div>

    <div class="group-right">
        <table class="student-table">
            <thead>
                <tr>
                    <th>Фамилия Имя Отчество</th>
                    <th>Номер группы</th>
                    <th>Год обучения</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in @Model.Students)
                {
                    <tr>
                        <td>@item.FIO</td>
                        <td>@item.GroupName</td>
                        <td>@item.StartYear</td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>